# Environment — NEVER commit secrets
.env
secrets/

# Let's Encrypt certs — managed by Traefik on server
traefik/acme.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Go debugger binary (generated by dlv on F5)
__debug_bin*

# Templ generated files (regenerated by `templ generate` or Dockerfile)
*_templ.go

# Logs
*.log

# Personal local overrides — use docker-compose.local.yml for machine-specific settings
docker-compose.local.yml

# AI context — local ops cheatsheet, never commit
docs/context.md

# Tailwind — downloaded binary only (style.css is committed as placeholder; CI regenerates it)
apps/personal-website/tailwindcss.exe